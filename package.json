{
  "name": "neon-tide",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --minify false --outDir dist-unminified && node tests/assert-build-output.mjs dist-unminified",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build",
    "test:production-probe": "node tests/browser-production-probe.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
